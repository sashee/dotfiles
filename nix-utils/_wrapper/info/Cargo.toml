[package]
name = "nix-sandbox-info"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
walkdir = "2"
libc = "0.2"
socket2 = "0.5"

[lints.rust]
unsafe_code = "forbid"
